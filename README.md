# Semantic-Cluster-WebUI
 Semantic-Cluster-WebUI clusters images with vision embeddings (DINOv2/CLIP), generates labels via VLM (Qwen2-VL), and organizes files by cluster. Built with Gradio for easy use.
